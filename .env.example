# Base URL of the FastAPI backend (no trailing slash). Leave empty for same-origin.
# Local dev points at the `serve` API; production is set in the deploy workflow.
VITE_API_BASE_URL=http://localhost:8000
